{
  "name": "vocabulary_support",
  "description": "Provide Japanese hints for difficult English words. Call this when the meaning of English terms is unclear: it returns candidate Japanese meanings for the important words of the text, so the caller can pick the sense that fits the context. It is a sidecar for disambiguation, not a translation service.",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The English text whose vocabulary should be explained. May be a word, a phrase, a sentence, or a paragraph."
      },
      "word": {
        "type": "string",
        "description": "A single word to resolve against the text. When given, only that word is explained instead of every important word of the text."
      },
      "max_terms": {
        "type": "integer",
        "description": "Maximum number of words to explain. Defaults to 20.",
        "minimum": 1,
        "maximum": 100
      }
    },
    "required": ["text"]
  }
}
