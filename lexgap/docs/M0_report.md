# LexGap M0 調査報告書

| 項目 | 内容 |
|---|---|
| 版 | v1.1 |
| 実施日 | 2026-09-22 |
| 対象 | マイルストーン M0（環境確認） |
| 計画書 | `docs/spec/LexGap（小型LLM日本語語彙欠落検証システム）実装計画書_20260921.md` |
| 状態 | **完了**（M0-1〜M0-6、Q-01・Q-02・Q-03 解消、環境チェック全12項目合格） |

本書は計画書 §5 M0 の成果物（計測メモ・調査メモ）をまとめたものである。数値はすべて実測であり、推定には実測値の算出根拠を併記した。

---

## 1. 結論

| 項目 | 結果 |
|---|---|
| M0-1 環境構築 | 完了。Python 3.12.3、依存4種＋pytest導入、`tkrzw-dict-agent` を editable 参照 |
| M0-2 DB疎通・走査時間 | 完了。全499,750キーを **12.1秒**（キー読取のみ）、レコード復号＋分野推定を含め **97秒** |
| M0-3 頻度フィールド（Q-01） | **解消**。`probability` を採用。ただし `1e-07` で左打ち切り。層化は `freq_rank` を使用 |
| M0-4 例文-語義対応（Q-02） | **解消**。例文はエントリ単位。語義単位の対応は**不可能** → 計画書のフォールバックを採用 |
| M0-5 ランナー疎通 | 完了。MLXサーバが応答、**seed再現・思考無効化に対応**。logprobsは非対応。Q-03確定 |
| M0-6 スキーマ初期化 | 完了。10テーブル、schema v1、冪等、WAL有効 |
| 環境チェック | `scripts/m0_envcheck.py` が**全12項目OK**（終了コード0） |

---

## 2. M0-1 環境

計画書 §2.1 の依存のうち、`tkrzw>=0.1`（C++バインディング）は**採用しなかった**。理由は §6 に記す。

| パッケージ | 導入版 |
|---|---|
| Python | 3.12.3 |
| regex | 2026.9.10 |
| httpx | 0.28.1 |
| numpy | 2.5.3 |
| scikit-learn | 1.9.1 |
| pytest | 9.1.1 |
| tkrzw-dict-agent | 0.1.0（editable, 親リポジトリ） |

`requirements.txt` は計画書の記載から `tkrzw` を除き、親パッケージを editable で参照する方式に変更した。

---

## 3. M0-2 走査性能

`union-keys.txt`（499,750行）が本体レコード数と完全一致し、かつ頻度降順であることを確認した。このため走査はキー一覧を順に `GetStr` する方式を採る（ハッシュバケット走査を自前実装するより単純で、頻度順が無償で得られる）。

| 処理 | 実測 | 備考 |
|---|---|---|
| 全キー読取のみ | **12.1秒**（24 µs/キー） | JSON復号なし |
| 全件走査＋レコード復号＋分野推定 | **97秒**（194 µs/キー、5,157件/秒） | `dict-stats` |
| 先頭20,000キー（高頻度＝巨大レコード） | 19.3秒（966 µs/キー） | 先頭ほど1件が重い |

分野推定（`guess_domain`）が全走査時間の約87%を占める。標本抽出は1回限りの処理であり、プローブごとのコストではないため許容するが、`sampler` 実装時は分野推定を必要とする層（`aux_domains`）だけに限定する最適化余地がある。

**エントリ数**: キー499,750に対しエントリ501,069（1キーが複数エントリを持つ場合がある。例: `japan` → `Japan` と `japan`）。

---

## 4. M0-3 頻度フィールド（Q-01）— 解消

### 4.1 候補フィールドの実測被覆率

| フィールド | 出現率 | 範囲 |
|---|---|---|
| **`probability`** | **100.0%**（501,069/501,069） | 0 〜 0.625、中央値 4e-07 |
| `aoa_concept` | 13.3% | 3.27 〜 27.9 |
| `share` | 1.9% | 0.125 〜 0.953 |
| `aoa_base` | 1.1% | 5.13 〜 23.9 |

`probability` 以外は被覆率が低すぎて主特徴量にできない。

### 4.2 打ち切りの発見

`probability` は100%存在するが、**`1e-07` を報告下限とする左打ち切り**を受けている。最小の正値がちょうど `1e-07` であり、それ未満は `0` として記録される。

- `probability == 0` のエントリ: **190,234件（38.0%）**
- 正値の最小: `1e-07`

### 4.3 打ち切りの分布

打ち切りは一様ではなく、**下位30%（頻度順位）に集中**する。

| 頻度順位 | `probability == 0` の割合 |
|---|---|
| 0〜40% | 約0% |
| 40〜60% | 約0% |
| 60〜70% | 79.5% |
| 70〜80% | 100% |
| 80〜100% | 100% |

### 4.4 解消内容

`log(probability)` で20分位に分割すると、辞書の38%が最終分位に集中し `per_quantile=100` の均衡が崩れる。

**採用方針**: 分位は `union-keys.txt` の**頻度順位 `freq_rank`** で切る。キー一覧は全語を被覆し、打ち切り領域内の順序も保持する。`log_freq` は `log10(max(probability, 1e-07))` として予測器の特徴量に残す（左打ち切り値 −7.0）。

計画書が未解消時フォールバックとして挙げた外部頻度表（wordfreq等）は**不要**である。辞書内部に完全な順序情報が存在するため、外部依存を追加しない。

---

## 5. M0-4 例文-語義対応（Q-02）— 解消

### 5.1 実測

| 項目 | 実測 |
|---|---|
| 例文オブジェクト総数 | **1,186,371** |
| 例文に語義スコープの標識を持つもの | **0** |
| 例文を持つエントリ | 328,033（65.5%） |
| 例文オブジェクトの構造 | `{"e": 英文, "j": 和訳}` のみ |
| `item`（語義）を持つエントリ | 501,069（100%） |
| 語義総数 | 1,036,050 |
| `[translation]:` を持つ語義 | 223,665（21.6%） |

### 5.2 解消内容

例文は**エントリ単位**であり、特定の語義に紐付ける情報を持たない。

**採用方針**: 条件C4（語義単位ゲーティング）は例文→語義対応を用いない。計画書 §6 のフォールバックに従い、**スコア上位語義で代用**する。

P2 の正解肢生成（計画書 §4.4）も同様に、計画書が示した「対応不明の場合はスコア上位語義」の経路を既定とする。

### 5.3 付随して判明した制約

- **訳語を持たないエントリが14.2%**（71,176件）存在する。語義側の `[translation]:` からも回復できるのは26件のみである。
- 訳語の無い語は**正解が定義できず判定不能**であるため、プローブ対象から除外する（`require_translation = true`）。
- 高頻度層（先頭20,000キー）では訳語被覆率が99.8%であり、主要な標本層では除外の影響は小さい。

---

## 6. 計画書からの逸脱

| 項目 | 計画書 | 実装 | 理由 |
|---|---|---|---|
| 依存 `tkrzw>=0.1` | C++バインディングを直接使用 | **不採用**。`tkrzw-dict-agent` 経由で `pytkrzw` を使用 | 親プロジェクトがC++を排除しPure Python化済み。同一リポジトリ内で両方式を混在させない |
| 頻度の代替 | 未解消時は外部頻度表（wordfreq等） | **不要**。内部の `freq_rank` を使用 | §4.4 |
| 分位の基準 | 対数頻度 | **頻度順位 `freq_rank`** | `probability` の左打ち切りにより対数頻度では均衡が崩れるため |
| C4（語義単位） | 例文の語義対応を前提 | **スコア上位語義で代用** | §5.2 |
| 配置 | `~/lexgap` | `~/src/tkrzw_dict_agent/lexgap` | 利用者判断（親リポジトリ内サブプロジェクト） |

---

## 7. M0-5 ランナー（Q-03 解消）

利用者指定のエンドポイントとモデルを実測で検証した。

| 項目 | 実測 |
|---|---|
| エンドポイント | `http://100.70.13.83:1234/v1`（Tailscale 経由） |
| サーバ | uvicorn、`owned_by: omlx`（MLX推論サーバ） |
| 提供モデル数 | 8 |
| 指定モデル | `Qwen3.8-27B-TURBO-Fable-Cold-Fusion-735-882-Heretic-Uncensored-NM-DAU-mlx-oQ8e-1M` |
| コンテキスト長 | `max_model_len = 1,048,576`（1M） |
| 生成 | 成功 |
| **seed再現** | **対応**（seed=42 の2回実行が完全一致） |
| `logprobs` | **非対応**（要求は受理されるが空オブジェクトを返す） |
| **思考の分離** | **なし**（`message` は `role` と `content` のみ） |

### 7.1 最重要の発見: 思考が `content` に混入する

指定モデルは推論（reasoning）モデルであり、既定のテンプレートでは思考過程が `message.content` にそのまま出力される。

```
既定:            'The user is asking for a one-word Japanese translation of
                  "Tightening."\n\nLet me think about the context. ...'
                 → 200トークン消費、訳語は得られず
enable_thinking=false: '締め付け'
                 → 2トークン、訳語のみ
```

判定器は出力を語義集合と照合するため、思考が混入した出力は比較不能である。

**対応**: 全プローブ要求で `chat_template_kwargs = {"enable_thinking": false}` を送る。`configs/runners.toml` の `disable_thinking_kwargs` に定義し、`envcheck` が「思考無効化で正常応答するか」を検証項目に含めている（合格済み）。トークン消費も約1/100になるため、コスト見積もりにも有利に働く。

### 7.2 logprobs 非対応

計画書 §4.4 の任意計測「平均対数確率」は本ランナーでは取得できない。`supports_logprobs = false` として記録した。

- 影響: 特徴量 `avg_logprob`（計画書 §4.6）は使用不可
- 代替: `--sc`（温度0.7×3回の自己整合性）は利用可能であり、不確実性の代理指標として `self_consistency` を使う

`envcheck` は logprobs を**必須項目ではなく記録項目**として扱う（`ok=True` 固定・詳細に可否を明記）。

### 7.3 単一モデル制約（M3影響）

MLXサーバは**同時に1モデルしか保持できない**。Metal の wired メモリ上限が 51.84GB で、指定モデルが 33.03GB を占めるため、他モデルのロードは上限超過で失敗する。

```
Qwen3.8-27B-MLX-6bit      → projected 55.31GB > 51.84GB で失敗
Qwen3.8-27B-Huihui-...    → projected 56.22GB > 51.84GB で失敗
Qwen3.8-Flash-Next-REAP-288 → projected 73.61GB > 51.84GB で失敗
```

**影響**: M3（量子化・モデル間比較）と M4（介入評価）は、モデルを**直列に切り替えながら**実行する必要がある。並列実行は不可能。`configs/models.toml` の各エントリに `resident` / `loadable` を記録した。

### 7.4 H4（量子化比較）の実施設計への影響

同一チェックポイントの BF16/Q8/Q4 という三つ組は**サーバ上に存在しない**。実測に基づく代替軸は2つ:

| 軸 | 対象 | 評価 |
|---|---|---|
| **枝刈り率** | `REAP-288` vs `REAP-384`（同一手法・同一ベース） | **推奨**。交絡が少ない |
| ビット幅 | `oQ8e` vs `oQ6e` vs `oQ3e-DWQ` | ファインチューンが異なるため**交絡あり**。その旨を明記して報告 |

### 7.5 Q-04（日本語劣化モデル）の候補

枝刈りモデル `Qwen3.8-Flash-Next-REAP-288/384` が Q-04 の候補として利用可能である（計画書の「枝刈り or 日本語の弱い公開モデルで代替」に合致）。ただし §7.3 のとおり、使用時は指定モデルをアンロードする必要がある。

---

## 8. M0-6 スキーマ

`init-db` により10テーブルを作成（冪等、WAL有効、外部キー有効、schema v1）。

```
sample_sets, samples, models, probes, judgments, audit_tasks,
features, predictions, intervention_runs, meta
```

設計上の要点:

- `probes` の UNIQUE 制約 `(set_id, headword, model_id, task, prompt_ver)` が**キャッシュそのもの**。再実行時は挿入前 SELECT で既存を検出し、モデル呼び出しをスキップする。
- トランザクション単位は**1プローブ**。失敗時はロールバックし、部分書き込みを残さない（テストで検証済み）。
- `PRAGMA journal_mode=WAL` により、レポータ（読み）がプローブ実行（書き）をブロックしない。

---

## 9. 検収

| 計画書の検収項目 | 結果 |
|---|---|
| `scripts/m0_envcheck.py` が全項目OK | **全12項目OK（終了コード0）** |
| 同一シードで抽出語集合が一致（AC4の一部） | **M1-2 で検証予定**（sampler 実装後） |
| `init_db()` 実行済み | 完了 |
| `specs/s1.toml` がコミット済み | 完了（`freq_rank` 層化・訳語必須を反映） |

**テスト**: 47件すべて合格（`config` / `db` / `dict_adapter` / `normalizer`）。

---

## 10. M1 への引き継ぎ

M1 の着手条件は満たしている（ブロッカーなし）。M1 実装時の必須事項:

1. **プローブ要求に `chat_template_kwargs = {"enable_thinking": false}` を必ず含める**（§7.1）。欠落すると思考が `content` に混入し判定不能になる
2. `avg_logprob` 特徴量は使用不可。`self_consistency`（`--sc`）を不確実性指標として使う（§7.2）
3. `sampler.py` は `freq_rank` で20分位、`require_translation = true` で抽出する（§4.4、§5.3）
4. `prompter.py` + `p1_v1.txt` を作成する。1M コンテキストが使えるため、例文と文脈を十分に含められる
5. `runner.py` は `probes` の UNIQUE 制約をキャッシュとして使い、並列度2・指数バックオフ3回で実装する。`seed` は固定値（42 等）を送る
6. `judge.py` は `dict_adapter.gloss_set()` と `normalizer.normalize()` を用い、K/P/U/X を付与する。規則3・4は区別せず監査キューへ送る（計画書 §4.5）
7. トークン消費は思考無効化により小さい（1語応答で2トークン実測）。計画書 §9.2 のコスト見積もりは M1-5 の実測で更新する

---

## 付録: 再現手順

```bash
cd ~/src/tkrzw_dict_agent/lexgap
.venv/bin/python3 scripts/m0_investigate.py      # Q-01/Q-02 の生データ
.venv/bin/python3 lexgap_cli.py dict-stats       # 被覆率レポート
.venv/bin/python3 scripts/m0_envcheck.py         # 環境チェック
```
